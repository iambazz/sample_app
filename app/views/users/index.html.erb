<h1>All users</h1>

<% if signed_in? %>
<div class="sidebar round" style="margin-top:-29px;">
	<%= render 'shared/user_links' %>
</div>
<% end %>

<%= will_paginate %>
<ul class="users">
	<%= render :partial => 'user', :collection=>@users %>
</ul>

<%= will_paginate %>