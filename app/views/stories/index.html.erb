
			<% if signed_in? %>
			<div class="sidebar round">
				<%= render 'shared/user_links' %>
			</div>
			<% end %>
			<% will_paginate %>
			<%= render :partial =>'story', :collection => Story.all %>
			<% will_paginate %>